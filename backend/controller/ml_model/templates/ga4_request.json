{
  "clientId": "${client_id}",
  {% if uses_first_party_data %}
  "userId": "${user_id}",
  {% endif %}
  "nonPersonalizedAds": false,
  "events": [
    {
      "name": "${event_name}",
      "params": {
        "type": "${type}",
        "value": "${value}",
        {% if label.is_score %}
        "score": "${score}",
        "nscore": "${normalized_score}"
        {% elif label.is_revenue %}
        "revenue": "${revenue}"
        {% endif %}
      }
    }
  ]
}
